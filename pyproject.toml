[project]
name = "rossmann"
version = "0.1.0"
description = "Nieoficjalne interfejsy CLI do sklepów rossmann.pl i allegro.pl"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "click>=8.1",
    "playwright>=1.45",
]

[project.scripts]
rossmann = "rossmann.cli:run"
allegro = "allegro.cli:run"

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["rossmann", "allegro"]

[tool.uv]
exclude-newer = "1 week"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
